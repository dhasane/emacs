;;; encryption.el --- Summary  -*- lexical-binding: t; -*-
;;; Commentary:
;;; Encryption and EPA configuration

;;; Code:

(require 'epa-file)
;; (custom-set-variables '(epg-gpg-program  "/usr/local/bin/gpg2"))
;; (custom-set-variables '(epg-gpg-program  "/opt/homebrew/bin/gpg"))
(epa-file-enable)

;;; encryption.el ends here
